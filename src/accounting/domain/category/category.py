class Category :
    def __init__(self, name: str, transactions: list(Transaction), userId: userId):
        self._id = 
        self._name = name
        self._transactions = transactions
        self._userId = userId
